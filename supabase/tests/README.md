# Testes RLS

Testes SQL com pgTAP para validar policies de RLS e Storage contra o Supabase local.

```bash
supabase start
supabase test db supabase/tests
```

Também é possível rodar pelo npm:

```bash
npm run test:rls
```
